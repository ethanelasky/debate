# Scheduler test ownership

The executable preimplementation contract gate and the dispositions for the
retired scheduler test sketches now live in the private
`ethanelasky/job-scheduler` repository.

The removed sketches fixed unapproved Python APIs and never constituted
conformance evidence. Future tests in this Debate repository should cover only
Debate-owned integration surfaces at the scheduler process/CLI boundary.
