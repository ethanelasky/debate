# OLMo-3-7B-Instruct-DPO proposer-critic on MATH level 5, all-on-pod via verl.
# Thinking OFF; private scratchpad slots replace <think>: proposal turn gets
# 1000 plan + 1000 argument, later speeches 500 + 500, judge 1000 + verdict.
# Judge = frozen Qwen3.5-4B served by a local vLLM on the pod (see
# scripts/pod_run.sh). Standalone entry on purpose: protocol turn lists
# merge BY INDEX under _extends, which poisons mixed-shape protocols.
#
# Run (on the pod): debate-rl --experiment-file configs/math_pc_olmo.yaml --experiment math_pc_olmo_l5
_includes: [_olmo_training.yaml]

math_pc_olmo_l5:
  _extends: _olmo_train_math
  protocol:
    turns:
      # `plan`, not `scratchpad`: the pre-proposal slot has its own cue
      # (spliced <PLAN_USER>) carrying the problem — the proposer has no
      # pre_debate preamble, so the generic scratchpad cue left alice
      # planning blind (2026-08-06). Later scratchpads keep the generic cue:
      # by then the problem/dispute is in context.
      - alice: [{name: plan, visibility: private, max_total_tokens: 1000},
                {name: proposal, kind: solution, max_total_tokens: 1000}]
      - bob:   [{name: scratchpad, visibility: private, max_total_tokens: 500},
                {name: critique, max_total_tokens: 500}]
      - alice: [{name: scratchpad, visibility: private, max_total_tokens: 500},
                {name: defense, max_total_tokens: 500}]
      - bob:   [{name: scratchpad, visibility: private, max_total_tokens: 500},
                {name: rebuttal, max_total_tokens: 500}]
      - judge: [{name: deliberation, visibility: private, max_total_tokens: 1000},
                {name: verdict, kind: decision, max_total_tokens: 256}]
  prompt_config:
    file_path: infra/prompts/debate/hendrycks_math.yaml
    entry: math_proposer_critic
  fresh_positions: true
  # RLVR evals run plan-then-answer (same shape as alice's plan slot, same
  # 1000-token cap) so the policy is measured in the mode it is trained in.
  plan_tokens: 1000
  agents:
    alice:
      trained: true
      model_settings:
        model_type: local            # served by the pod's verl rollout engine
        model_file_path: allenai/Olmo-3-7B-Instruct-DPO
        alias: proposer-alice
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    bob:
      trained: true
      model_settings:
        model_type: local
        model_file_path: allenai/Olmo-3-7B-Instruct-DPO
        alias: critic-bob
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    judge:
      model_settings:
        model_type: local
        model_file_path: Qwen/Qwen3.5-4B
        alias: judge
        base_url: http://127.0.0.1:8788/v1
        # temp/top_p 1.0: the logit confidence channel is only honest here
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
  judge_config: {schema_name: competitive, retries: 4}
  scoring:
    scoring: continuous
    confidence_source: json
    shaping:
      - {kind: format_reward, coeff: 0.1, slots: [proposal], flag: strict_boxed}
  dataset: {type: math, levels: 5, relaxed_extraction: true, seed: 0}
  training:
    verl:
      n_gpus: 1                      # H100/A100-80GB: 7B LoRA FSDP + colocated vllm + judge server
      gpu_memory_utilization: 0.33   # judge 0.18 + rollout 0.33 + 7B FSDP init peak must fit 80GB
      prompt_length: 8192
      response_length: 1024          # largest slot cap

# tiny live-verification arm
math_pc_olmo_smoke:
  _extends: math_pc_olmo_l5
  training:
    steps: 2
    batch_size: 2
    group_size: 4   # group 2 risks all-identical verdicts -> every datum dropped
    eval_every: 0
    save_every: 0

# Mirror of the successful math_rlvr_olmo_l5 run (wandb debate-rebuild,
# 2026-08-04, final eval 77.3%): group 8, lr 1e-4, and the proposal budget
# raised to RLVR's 2000-token completion cap — the post-ff3820c step-by-step
# answer prompt runs long, and 1000 truncated proposals would poison the judge
# signal. Standalone entry, same reason as math_pc_olmo_l5: protocol turn
# lists merge BY INDEX under _extends.
math_pc_olmo_l5_g8:
  _extends: _olmo_train_math
  protocol:
    turns:
      # plan (not scratchpad): see math_pc_olmo_l5's protocol comment.
      - alice: [{name: plan, visibility: private, max_total_tokens: 1000},
                {name: proposal, kind: solution, max_total_tokens: 2000}]
      - bob:   [{name: scratchpad, visibility: private, max_total_tokens: 500},
                {name: critique, max_total_tokens: 500}]
      - alice: [{name: scratchpad, visibility: private, max_total_tokens: 500},
                {name: defense, max_total_tokens: 500}]
      - bob:   [{name: scratchpad, visibility: private, max_total_tokens: 500},
                {name: rebuttal, max_total_tokens: 500}]
      - judge: [{name: deliberation, visibility: private, max_total_tokens: 1000},
                {name: verdict, kind: decision, max_total_tokens: 256}]
  prompt_config:
    file_path: infra/prompts/debate/hendrycks_math.yaml
    entry: math_proposer_critic
  fresh_positions: true
  # RLVR evals run plan-then-answer (same shape as alice's plan slot, same
  # 1000-token cap) so the policy is measured in the mode it is trained in.
  plan_tokens: 1000
  agents:
    alice:
      trained: true
      model_settings:
        model_type: local
        model_file_path: allenai/Olmo-3-7B-Instruct-DPO
        alias: proposer-alice
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    bob:
      trained: true
      model_settings:
        model_type: local
        model_file_path: allenai/Olmo-3-7B-Instruct-DPO
        alias: critic-bob
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    judge:
      model_settings:
        model_type: local
        model_file_path: Qwen/Qwen3.5-4B
        alias: judge
        base_url: http://127.0.0.1:8788/v1
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
  judge_config: {schema_name: competitive, retries: 4}
  scoring:
    scoring: continuous
    confidence_source: json
    shaping:
      - {kind: format_reward, coeff: 0.1, slots: [proposal], flag: strict_boxed}
  dataset: {type: math, levels: 5, relaxed_extraction: true, seed: 0}
  training:
    backend: verl
    verl:
      n_gpus: 2                      # 2xH100 pod (4x had no US-CA-2 stock 2026-08-05);
                                     # judge still colocates on GPU 0 at 0.18
      strategy: fsdp2
      checkpoint_dir: /workspace/checkpoints
      gpu_memory_utilization: 0.33
      prompt_length: 8192
      response_length: 2048          # largest slot cap (proposal 2000)

# CoT-discovery arm: neutral prompt (math_nocot.yaml — no step-by-step ask, no
# answer-only ban). The nocot answer prompt splices into the proposal slot via
# <ANSWER_GEN_USER>, byte-identical to math_rlvr_olmo_l5_nocot's user message.
# Question: does judge-mediated debate reward discover derivations when the
# prompt is silent on them — and faster/higher than the verifier reward does?
math_pc_olmo_l5_g8_nocot:
  _extends: math_pc_olmo_l5_g8
  dataset: {type: math, levels: 5, relaxed_extraction: true, seed: 0, prompt_file: infra/prompts/tasks/math_nocot.yaml}

# OLMo-3-32B Think-DPO debate arm (Ethan, 2026-08-11). This is a controlled
# splice of two completed/live runs, not a new debate recipe:
#
#   debate protocol/dataset/reward: math_pc_olmo_l5_g8_nocot
#     W&B palaestra-research/debate-rebuild/bkih4xbk (latest completed debate)
#   optimizer recipe: aime_rlvr_olmo32think_cispo_os3
#     W&B palaestra-research/debate-math/zeaz96rn (live 32B run)
#
# _extends deliberately preserves the debate's five serial stages and every
# per-slot cap. The overrides below change only the shared policy checkpoint,
# 32B topology, and training recipe. gpu_memory_utilization stays at the
# debate-safe 0.33 rather than the judge-free RLVR run's 0.42: the frozen 4B
# judge reserves another 0.18 of GPU 0 on the same 183GB B200.
# STANDALONE, not _extends (2026-08-12): this arm's protocol SHAPE differs
# from the nocot parent's (think-native, no plan/scratchpad turns), and
# protocol turn lists merge BY INDEX under _extends — the exact poison the
# other standalone entries document. Everything except the protocol is a
# verbatim copy of math_pc_olmo_l5_g8_nocot's semantics.
math_pc_olmo32think_cispo_os3:
  # Think-native protocol (Ethan's spec, 2026-08-12): the model's PRIVATE
  # <think> phase replaces every scratchpad/plan turn — 1k think + 500
  # visible per debater slot, solution first (required by the solo-opening
  # flag; a plan turn would double the private channel). Judge unchanged.
  protocol:
    turns:
      - alice: [{name: proposal, kind: solution, max_think_tokens: 1000, max_total_tokens: 1500}]
      - bob:   [{name: critique, max_think_tokens: 1000, max_total_tokens: 1500}]
      - alice: [{name: defense, max_think_tokens: 1000, max_total_tokens: 1500}]
      - bob:   [{name: rebuttal, max_think_tokens: 1000, max_total_tokens: 1500}]
      - judge: [{name: deliberation, visibility: private, max_total_tokens: 1000},
                {name: verdict, kind: decision, max_total_tokens: 256}]
  prompt_config:
    file_path: infra/prompts/debate/hendrycks_math.yaml
    entry: math_proposer_critic
  fresh_positions: true
  # The proposer's opening is a SOLO solution — rendered without any debate
  # context (Ethan, 2026-08-12): the opening must share the RLVR twin's
  # prompt distribution; debate-awareness enters with the critic's speech.
  first_speech_non_debate_aware: true
  plan_tokens: 1000
  agents:
    alice:
      trained: true
      model_settings:
        model_type: local
        model_file_path: allenai/Olmo-3-32B-Think-DPO
        alias: proposer-alice
        enable_thinking: true
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    bob:
      trained: true
      model_settings:
        model_type: local
        model_file_path: allenai/Olmo-3-32B-Think-DPO
        alias: critic-bob
        enable_thinking: true
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    judge:
      model_settings:
        model_type: local
        model_file_path: Qwen/Qwen3.5-4B
        alias: judge
        base_url: http://127.0.0.1:8788/v1
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
  judge_config: {schema_name: competitive, retries: 4}
  scoring:
    scoring: continuous
    confidence_source: json
    shaping:
      - {kind: format_reward, coeff: 0.1, slots: [proposal], flag: strict_boxed}
  dataset: {type: math, levels: 5, relaxed_extraction: true, seed: 0, prompt_file: infra/prompts/tasks/math_nocot.yaml}
  training:
    backend: verl
    eval_max_tokens: 2000
    wandb_entity: palaestra-research
    wandb_project: debate-math
    lora_rank: 32
    loss: {kind: cispo, clip_low: 0.8, clip_high: 1.28}
    ppo_epochs: 2
    adv_length_norm: none
    steps: 100
    batch_size: 8
    group_size: 8
    lr: 1.0e-4
    kl_coef: 0.02
    kl_discount_factor: 0.0
    eval_every: 10
    eval_n: 93
    save_every: 10
    dynamic_sampling_retries: 0
    oversample_factor: 3
    kl_mechanism: loss
    drop_zero_advantage: false
    eval_split: dev
    final_test_eval: true
    warmup_steps: 10
    lr_schedule: cosine
    min_lr_ratio: 0.2
    verl:
      n_gpus: 2
      strategy: fsdp2
      rollout_tp: 2
      checkpoint_dir: /workspace/checkpoints
      gpu_memory_utilization: 0.33
      prompt_length: 8192
      response_length: 2048
      max_token_len_per_gpu: 16384
      extra_overrides:
        - "++actor_rollout_ref.rollout.engine_kwargs.vllm.disable_custom_all_reduce=true"
        - "++actor_rollout_ref.actor.loss_agg_mode=seq-mean-token-mean"
        - "++actor_rollout_ref.actor.optim.clip_grad=0.5"

# OLMo-32B INSTRUCT debate arm on AIME (Ethan, 2026-08-12): strong instruct
# debaters + the weak 4B judge — the debate twin of
# aime_rlvr_olmo32_plan1k_cispo_os3 (the live instruct RLVR arm).
# STANDALONE like the think arm above (protocol turn lists merge BY INDEX
# under _extends). Same five-stage shape and per-slot TOTAL budgets as the
# think arm; the think/visible split disappears because the instruct model
# has no think channel — it derives inline in the visible speech, which is
# exactly what the critic attacks.
#   - debaters: the bf16 volume conversion (the HF repo ships fp32; loading
#     that under TP2 OOM-kills the rollout workers — see aime_rlvr_olmo32)
#   - dataset {type: aime}: AimeFamily reuses math.yaml's answer prompt, so
#     the solo opening is byte-identical to the RLVR twin's answer turn
#   - eval: plan_tokens 1000 + eval_max_tokens 1000 mirrors the RLVR twin's
#     direct-eval shape (plan 1k, answer cap inherited 1000)
#   - THROUGHPUT deviations from the twin (Ethan, 2026-08-13, ~20min/step was
#     too slow): oversample 2 not 3 (first steps showed 0-6 degenerates per
#     192 debates — the third draw was waste) and a 300-token rebuttal.
#     gmu stays 0.33: the 0.45 attempt (run 0j24zfrk) died in step 1 on a
#     vLLM 0.24 scheduler KeyError (req_id_to_index) — the only engine knob
#     that changed; 0.33 has ~19 crash-free debate steps behind it.
aime_pc_olmo32_cispo_os2:
  protocol:
    turns:
      - alice: [{name: proposal, kind: solution, max_total_tokens: 1500}]
      - bob:   [{name: critique, max_total_tokens: 1500}]
      - alice: [{name: defense, max_total_tokens: 1500}]
      - bob:   [{name: rebuttal, max_total_tokens: 300}]
      - judge: [{name: deliberation, visibility: private, max_total_tokens: 1000},
                {name: verdict, kind: decision, max_total_tokens: 256}]
  prompt_config:
    file_path: infra/prompts/debate/hendrycks_math.yaml
    entry: math_proposer_critic
  fresh_positions: true
  # Solo opening: rendered without any debate context so it shares the RLVR
  # twin's prompt distribution; debate-awareness enters with the critique.
  first_speech_non_debate_aware: true
  plan_tokens: 1000
  agents:
    alice:
      trained: true
      model_settings:
        model_type: local
        model_file_path: /workspace/models/olmo32-bf16
        alias: proposer-alice
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    bob:
      trained: true
      model_settings:
        model_type: local
        model_file_path: /workspace/models/olmo32-bf16
        alias: critic-bob
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    judge:
      model_settings:
        model_type: local
        model_file_path: Qwen/Qwen3.5-4B
        alias: judge
        base_url: http://127.0.0.1:8788/v1
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
  judge_config: {schema_name: competitive, retries: 4}
  scoring:
    scoring: continuous
    confidence_source: json
    shaping:
      - {kind: format_reward, coeff: 0.1, slots: [proposal], flag: answer_format_valid}
  dataset: {type: aime, seed: 0}
  training:
    backend: verl
    eval_max_tokens: 1000
    wandb_entity: palaestra-research
    wandb_project: debate-math
    lora_rank: 32
    loss: {kind: cispo, clip_low: 0.8, clip_high: 1.28}
    ppo_epochs: 2
    adv_length_norm: none
    steps: 100
    batch_size: 8
    group_size: 8
    lr: 1.0e-4
    kl_coef: 0.02
    kl_discount_factor: 0.0
    eval_every: 10
    eval_n: 93
    save_every: 10
    dynamic_sampling_retries: 0
    oversample_factor: 2
    kl_mechanism: loss
    drop_zero_advantage: false
    eval_split: dev
    final_test_eval: true
    warmup_steps: 10
    lr_schedule: cosine
    min_lr_ratio: 0.2
    verl:
      n_gpus: 2
      strategy: fsdp2
      rollout_tp: 2
      checkpoint_dir: /workspace/checkpoints
      gpu_memory_utilization: 0.33
      prompt_length: 8192
      response_length: 2048
      max_token_len_per_gpu: 16384
      extra_overrides:
        - "++actor_rollout_ref.rollout.engine_kwargs.vllm.disable_custom_all_reduce=true"
        - "++actor_rollout_ref.actor.loss_agg_mode=seq-mean-token-mean"
        - "++actor_rollout_ref.actor.optim.clip_grad=0.5"
