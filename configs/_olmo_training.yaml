# Shared `training:` block for the OLMo RLVR/PC pairs. Values are the RLVR
# side's; debate arms customize on top. Only `training:` — never `protocol:`
# (deep_merge merges lists BY INDEX) and never the RLVR experiment-level keys
# (absent from run_debate's EXPERIMENT_KEYS).

_olmo_train_base:
  training:
    backend: verl
    verl:
      strategy: fsdp2              # needs the modeling_olmo3 rope patch (provision_pod.sh)
      # network volume: adapters are ~100-200MB with save_lora_only (which the
      # backend forces whenever lora_rank > 0)
      checkpoint_dir: /workspace/checkpoints
    # The TEAM, not the api key's personal default entity — without this,
    # runs land in whoever launched them's own namespace and the rest of
    # the team cannot see them.
    wandb_entity: palaestra-research
    lora_rank: 32
    loss: {kind: ppo, clip_low: 0.8, clip_high: 1.2}
    ppo_epochs: 1
    # count: per-seat mass balance WITHOUT the per-token division (verl's
    # token-mean loss already normalizes scale; trajectory-mode would shrink
    # advantages ~1000x and demand absurd lrs)
    adv_length_norm: count
    steps: 100
    batch_size: 8
    group_size: 8      # GRPO groups are per (task, arm, trained seat); g8 halves advantage noise vs g4
    lr: 1.0e-4         # the RLVR lr that reached 77.3%; 1e-5 was dead here
    kl_coef: 0.02
    kl_discount_factor: 0.0
    eval_every: 5
    save_every: 25     # lora-only adapters (~100-200MB) on the network volume

_olmo_train_math:
  _extends: _olmo_train_base
  training:
    wandb_project: debate-math
    verl:
      max_token_len_per_gpu: 16384
    eval_n: 256        # +-3.1pp binomial noise; 128 (+-4.4pp) made 3pp trends uncallable
    eval_max_tokens: 2000

_olmo_train_codecontests:
  _extends: _olmo_train_base
  training:
    wandb_project: debate-rebuild
    # The whole held-out split. Only 116 rows survive filtering and
    # CodeContestsEnv.tasks does test_rows[:n], so a larger eval_n changes
    # nothing. +-4.6pp binomial noise: a floor/ceiling check, not a sensitive
    # metric.
    eval_n: 116
