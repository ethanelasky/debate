# RLVR baseline on CodeContests — the arm debate gets compared against.
#
# Reward = execute the model's program against that problem's `rlvr_tests`
# (<=10 cases sampled from DeepMind's public+private). No judge, no debate.
# Mirrors math_rlvr_olmo.yaml so the only difference from the MATH baseline is
# the task family.
#
# Dataset: build it first, it is gitignored (132 MB):
#   python scripts/build_codecontests_rlvr.py --out data/codecontests
# The manifest pins revision + seed + sha256, so the build is reproducible
# byte-for-byte rather than shipped.
#
# Run (on the pod): scripts/pod_run.sh rlvr codecontests_rlvr_olmo_smoke
_includes: [_olmo_training.yaml]

codecontests_rlvr_olmo:
  _extends: _olmo_train_codecontests
  model: allenai/Olmo-3-7B-Instruct-DPO
  max_completion_tokens: 2048
  dataset:
    type: codecontests
    path: data/codecontests/train.jsonl
    # Self-contained held-out intersection. Every row carries the same problem
    # plus explicitly named <=10-case GDM and CCO suites, so one greedy
    # completion produces overlayable eval/gdm_correct and eval/cco_correct.
    # There is no runtime sidecar join.
    #   python scripts/fetch_cco_eval.py
    #   python scripts/build_codecontests_paired_eval.py
    paired_test_path: data/codecontests/paired_test.jsonl
    relaxed_extraction: true   # accept a ```python fence truncated at the cap
    seed: 0
    # Bounds a runaway program, not honest work: the reward suite is <=10 small
    # cases (2 MB total budget, enforced at build time), so a correct solution
    # finishes in well under a second.
    timeout_seconds: 10
    # The complete paired intersection: 456 problems, still contest-disjoint
    # from train. Four CCO matches with conflicting expected outputs were
    # removed and 41 GDM held-out problems had no CCO match.
    eval_subset_size: 456
    expected_eval_size: 456
    # FLAT overshoot penalty: a completion past the budget loses a constant
    # 0.1, regardless of how far over. The old repo RAMPED this between a soft
    # 4096 and a hard 8192 (coef 0.10, so a half-way overshoot cost 0.05);
    # flat is the deliberate change, so the gradient says "stay under" rather
    # than "be marginally shorter" — the model learns the budget quickly.
    #
    # 1536 is measured, not guessed. Over 12,436 cached model completions on
    # these problems (the old repo's answer cache), approx tokens:
    #                 median   p90     p95    fits 1536   fits 2048
    #   CORRECT   (n=4988)  505   1,272  1,842     92.5%      96.3%
    #   INCORRECT (n=1590)  676   2,573  3,214     77.4%      85.2%
    # Correct answers are SHORTER than wrong ones, so a 1536 budget blocks few
    # correct policy updates while pricing a tail that skews wrong. Those
    # samples were 122B/35B-class though; if OLMo-7B rambles more, the
    # over_budget metric will show it in step 1.
    #
    # It only bites if response_length exceeds it — see below. It does NOT
    # move the sampler cap; generation still stops at response_length.
    soft_token_budget: 1536
    overshoot_penalty: 0.1
  training:
    verl:
      n_gpus: 1
      gpu_memory_utilization: 0.45 # no judge server here: rollout can take more than debate's 0.33
      # CodeContests statements are ~3x MATH: 474 tokens median, 1323 at p99,
      # 1850 max, plus the answer-gen preamble.
      prompt_length: 4096
      # Headroom over the budget so the penalty has something to price; 2048
      # still captures 96.3% of correct answers. A response_length at or below
      # soft_token_budget would make the penalty dead config.
      response_length: 2048
      max_token_len_per_gpu: 8192    # >= prompt_length + response_length
    # 8x8 = 64 rollouts/step. verl micro-batches against max_token_len_per_gpu,
    # so batch size costs wall-clock, not peak memory; the 16x8 arm below is
    # the same run at double the rollouts. Base arm is the cheaper one.
    batch_size: 8
    eval_max_tokens: 2048

# 1-step smoke: proves the whole path — dataset load, prompt render, rollout,
# code extraction, subprocess verifier, reward, optimizer step — before
# spending a real run.
codecontests_rlvr_olmo_smoke:
  _extends: codecontests_rlvr_olmo
  training:
    steps: 1
    batch_size: 2
    group_size: 4
    eval_every: 0
    save_every: 0

# Double the rollouts per step. Same memory profile (verl packs against
# max_token_len_per_gpu); ~2x the wall-clock.
codecontests_rlvr_olmo_16x8:
  _extends: codecontests_rlvr_olmo
  training:
    batch_size: 16

# Long-budget control. Only worth running if the smoke run shows OLMo's
# completions clustering well above 1536 — i.e. if the measured distribution
# above does not transfer from the 122B/35B-class samples it came from.
# ~5x the generation of the base arm.
codecontests_rlvr_olmo_long:
  _extends: codecontests_rlvr_olmo
  max_completion_tokens: 8192
  dataset:
    soft_token_budget: 4096
  training:
    verl:
      response_length: 8192
      max_token_len_per_gpu: 16384
    eval_max_tokens: 8192

# ---------------------------------------------------------------- 32B arm
# The real baseline. Matches the OLD repo's CodeContests canary
# (codecontests_olmo31_32b_canary_experiments.yaml): Olmo-3.1-32B-Instruct-DPO
# on 8 GPUs, lora_rank 32, gpu_mem_util 0.45, max_new_tokens 8192.
#
# NOTE the model family: 3.1, not the 3-7B the MATH baselines in this repo use.
# The arms above are 7B and exist to prove the code path cheaply; this is the
# one whose numbers mean anything.
#
# Why 8 GPUs: 32B in bf16 is ~64 GB of weights, so vLLM's rollout copy plus
# FSDP training state cannot share a single 80 GB card.
#
# The token budget goes back to the canary's 4096/8192 pair rather than the
# 1536/2048 measured for the 7B arms — a 32B is the size class those cached
# completions came from, so the short budget's evidence does not apply here.
codecontests_rlvr_olmo31_32b:
  _extends: codecontests_rlvr_olmo
  # Run-proven no-think OLMo-3.1-32B-Instruct-DPO at revision fc84a4f,
  # converted to BF16. The bare HF repository stores roughly 121 GB of FP32
  # weights and OOM-killed TP2 workers; this audited artifact is ~64.5 GB.
  model: /workspace/models/olmo32-bf16-legacy-audited-fc84a4f
  max_completion_tokens: 8192
  dataset:
    soft_token_budget: 4096
    overshoot_penalty: 0.1
  training:
    verl:
      n_gpus: 8
      # Shard the ROLLOUT across all 8 GPUs. Without this rollout_tp defaults
      # to 1 and vLLM tries to put the whole 32B (~64 GB bf16) on ONE card;
      # at gpu_memory_utilization 0.45 of a 143 GB H200 that is a 64 GB budget
      # for 64 GB of weights, so it dies with "No available memory for the
      # cache blocks" before a single token is generated. With tp=8 each card
      # holds ~8 GB of weights and the rest of its 45% budget is KV cache.
      rollout_tp: 8
      response_length: 8192
      max_token_len_per_gpu: 16384
    eval_max_tokens: 8192

# 2 steps with eval on every step: enough to draw a LINE in wandb rather than a
# single point, and enough to see the policy actually move (grad_norm, kl,
# entropy, reward between step 0 and 1). eval_n is small so the greedy pass
# plus verifier stays under a minute.
codecontests_rlvr_olmo_smoke2:
  _extends: codecontests_rlvr_olmo
  training:
    steps: 2
    batch_size: 2
    group_size: 4
    eval_every: 1
    eval_n: 8
    save_every: 0

# 10 steps at the REAL arm's rollout shape (8x8), so the curves reflect what a
# full run would look like rather than 2x4 noise. eval every 2 steps on 32
# held-out problems: enough points to see a trend, cheap enough to stay ~30min.
codecontests_rlvr_olmo_smoke10:
  _extends: codecontests_rlvr_olmo
  training:
    steps: 10
    batch_size: 8
    group_size: 8
    eval_every: 2
    eval_n: 32
    save_every: 0

# Cheap learning-signal test on the difficulty slice where the 7B inference
# smoke produced useful reward variance. Bounds are inclusive: [800, 1100]
# is Codeforces-rated problems below 1200. The lower bound is essential because
# GDM records unmatched/unrated problems as cf_rating=0; a bare max would pull
# 5,189 unknown-difficulty rows into the supposedly easy training set.
# Inventory: 916 train, 75 paired held out. The smoke saw 54.5% mixed G=8
# groups on the original 82-problem slice, versus 21.9% by ratings 1300-1400.
codecontests_rlvr_olmo_easy_smoke10:
  _extends: codecontests_rlvr_olmo_smoke10
  dataset:
    min_cf_rating: 800
    max_cf_rating: 1100
    eval_subset_size: 75
    expected_eval_size: 75

# Paired 25-step learning experiment on the viable 7B difficulty slice. Both
# arms see the same seeded training stream and complete 75-problem paired held-out
# split. Intermediate + final checkpoints make the policy movement inspectable
# even if correctness peaks before the last step.
codecontests_rlvr_olmo_easy25_penalty:
  _extends: codecontests_rlvr_olmo
  dataset:
    min_cf_rating: 800
    max_cf_rating: 1100
    eval_subset_size: 75
    expected_eval_size: 75
  training:
    steps: 25
    batch_size: 8
    group_size: 8
    eval_every: 5
    eval_n: 75
    save_every: 10

# Length-penalty ablation. The soft budget remains configured so over-budget
# incidence is still measured, but a zero coefficient removes it from reward.
# Everything else is byte-for-byte inherited from the paired control above.
codecontests_rlvr_olmo_easy25_no_penalty:
  _extends: codecontests_rlvr_olmo_easy25_penalty
  dataset:
    overshoot_penalty: 0.0

# Three-seed follow-up on the stricter slice suggested by the 25-step result:
# Codeforces ratings [800, 1000], i.e. strictly below 1100. This leaves 727
# train / 55 paired, contest-disjoint held-out problems. All seeds evaluate the
# same complete paired set at temperature 0; dataset.seed changes the training
# task stream while training.rl_seed changes GRPO packing/optimizer shuffles.
# eval_every=7 includes step 49, so the last plotted point is the final update
# rather than a post-hoc checkpoint evaluation with different plumbing.
codecontests_rlvr_olmo_easy1000_seed0_50:
  _extends: codecontests_rlvr_olmo
  dataset:
    min_cf_rating: 800
    max_cf_rating: 1000
    eval_subset_size: 55
    expected_eval_size: 55
    seed: 0
  training:
    steps: 50
    batch_size: 8
    group_size: 8
    eval_every: 7
    eval_n: 55
    save_every: 25
    log_transcripts: true
    rl_seed: 0

codecontests_rlvr_olmo_easy1000_seed1_50:
  _extends: codecontests_rlvr_olmo_easy1000_seed0_50
  dataset:
    seed: 1
  training:
    rl_seed: 1

codecontests_rlvr_olmo_easy1000_seed2_50:
  _extends: codecontests_rlvr_olmo_easy1000_seed0_50
  dataset:
    seed: 2
  training:
    rl_seed: 2

# Larger task batch on the same viable 7B slice. Group size remains eight, so
# this doubles independent problems and total rollouts per optimizer update
# (16x8 = 128) without changing the per-problem GRPO baseline. At the observed
# ~79% non-zero-advantage rate, this would cross the default micro_batch=64.
# Use 128 so VERL computes one global token denominator for the whole kept
# update; its internal max_token_len_per_gpu packing still bounds activations.
codecontests_rlvr_olmo_easy1000_b16_seed0_50:
  _extends: codecontests_rlvr_olmo_easy1000_seed0_50
  training:
    batch_size: 16
    micro_batch: 128

codecontests_rlvr_olmo_easy1000_b16_seed1_50:
  _extends: codecontests_rlvr_olmo_easy1000_seed1_50
  training:
    batch_size: 16
    micro_batch: 128

codecontests_rlvr_olmo_easy1000_b16_seed2_50:
  _extends: codecontests_rlvr_olmo_easy1000_seed2_50
  training:
    batch_size: 16
    micro_batch: 128

# One paid-compute boundary check before the three full runs: verifies that
# 128 rollouts and the wider logical microbatch fit, and that gradient norm
# and memory do not jump pathologically relative to the completed 8x8 runs.
codecontests_rlvr_olmo_easy1000_b16_smoke1:
  _extends: codecontests_rlvr_olmo_easy1000_b16_seed0_50
  training:
    steps: 1
    eval_every: 0
    save_every: 0
    log_transcripts: false

# 10 steps at the real 8x8 shape on the 32B. The first run whose numbers are
# worth reading: the 7B smoke showed the plumbing works but the model is too
# weak on CodeContests (eval/correct_relaxed ~0.03, a third of programs crashing).
codecontests_rlvr_olmo31_32b_smoke10:
  _extends: codecontests_rlvr_olmo31_32b
  training:
    steps: 10
    batch_size: 8
    group_size: 8
    eval_every: 2
    eval_n: 32
    save_every: 0

# Phase-timing run: 3 steps at the real 8x8 shape, NO eval, no save. Exists to
# fill in the phase/* panels (generate vs reward vs KL vs fwd/bwd vs sleep/wake)
# so the 141s residual in a 171s step stops being a guess. Keep it to 3 steps —
# step 0 carries init, so steps 1-2 are the ones that matter.
codecontests_rlvr_olmo31_32b_phases:
  _extends: codecontests_rlvr_olmo31_32b
  training:
    steps: 3
    batch_size: 8
    group_size: 8
    eval_every: 0
    save_every: 0

# 4-GPU variant of the phase run: 8xH200 had no capacity. 32B at TP=4 is
# 15GB of weights per 141GB card, leaving ~48GB for KV cache inside the 0.45
# budget. Phase PROPORTIONS shift vs TP=8 (less per-token all-reduce, fewer
# cards for compute) but the structure is what we are after.
codecontests_rlvr_olmo31_32b_phases4:
  _extends: codecontests_rlvr_olmo31_32b_phases
  training:
    verl:
      n_gpus: 4
      rollout_tp: 4

# Token-cap sweep. Baseline is codecontests_rlvr_olmo31_32b_smoke10:
# response_length 8192, soft budget 4096 (ratio 1/2). These keep that ratio so
# only the CAP varies.
#
# Offline on 640 real 8192-budget completions, capping truncates the final code
# block of anything ending past the cap:
#   cap    passing kept   pass rate   generation cost
#   1024        49.1%        22.2%          12%
#   2048        63.3%        28.6%          25%
#   8192       100.0%        45.2%         100%
# That bounds the IMMEDIATE cost of truncation. It cannot say how the model
# behaves when TRAINED under the cap — it may write tighter solutions (loss
# smaller than measured) or collapse into degenerate short outputs. That is
# what these runs are for.
codecontests_rlvr_olmo31_32b_cap2048:
  _extends: codecontests_rlvr_olmo31_32b_smoke10
  max_completion_tokens: 2048
  dataset:
    soft_token_budget: 1024
    # Both cap arms use the same isolated execution protocol. The pod must
    # establish this loopback SSH forward and pass smoke_piston_verifier.py
    # before model loading.
    verifier: piston
    piston_url: http://127.0.0.1:2000
    piston_python_version: 3.12.0
  training:
    verl:
      n_gpus: 4
      rollout_tp: 4
      response_length: 2048
      max_token_len_per_gpu: 8192
    eval_max_tokens: 2048

# The 2048 cap, run long enough to actually learn. The 10-step arms above
# settled the INFERENCE cost of each cap (2048 loses ~6pp of train correctness
# to the 8192 baseline, paired over the identical seeded problem stream, 9 steps
# of 10; 1024 loses ~12pp and never moved off 1/32 on eval). None of them said
# anything about TRAINING under a cap, because none of them trained: KL to the
# reference policy after 10 steps was 0.0013 nats, i.e. the step-9 model is the
# step-0 model. 640 rollouts over ~80 distinct problems at lr 1e-4 on a rank-32
# adapter cannot move a 32B, and ~30% of every batch is dropped for zero
# advantage on top of that (all-8-pass or all-8-fail groups teach nothing).
#
# eval_n goes back to the 256 the base config asks for. At n=32 the standard
# error on a ~0.13 accuracy is +-0.06 — two problems — so 13% and 19% are the
# same measurement, and a 100-step run would be unreadable. Every 10 steps
# rather than every 5: an eval at n=256 costs ~334s against a 60s training step,
# and mid-run eval points are not something we act on.
codecontests_rlvr_olmo31_32b_cap2048_long:
  _extends: codecontests_rlvr_olmo31_32b_cap2048
  training:
    verl:
      gpu_memory_utilization: 0.40   # 80 GB cards; see cap1024_long
    steps: 100
    eval_every: 10
    eval_n: 256
    save_every: 25
    # Match the current OLMo RL recipe: warm up to 1e-4 over ten updates,
    # then cosine-decay to 0.2x peak at the end of the 100-step run.
    warmup_steps: 10
    lr_schedule: cosine
    min_lr_ratio: 0.2

# The 1024 arm at the same length as cap2048_long, so the two are overlayable.
# The 10-step version looked dead — eval pinned at 1/32 for its whole run — but
# that run had no chance to show anything: KL to reference was 0.0035 at the end,
# i.e. the policy had not moved, and n=32 could not have resolved a change
# anyway. Whether a 1024 cap is trainable is genuinely open until it gets the
# same 100 steps and the same n=256 eval the 2048 arm got.
#
# Everything except run length is inherited from cap1024, so the pair differs
# only in the cap itself.
codecontests_rlvr_olmo31_32b_cap1024_long:
  _extends: codecontests_rlvr_olmo31_32b_cap1024
  training:
    verl:
      # Sized for 80 GB cards (H100), not the 143 GB H200 the earlier arms ran
      # on. 0.45 x 80 = 36 GB for vLLM against 16 GB of sharded weights (64 GB
      # bf16 / rollout_tp 4); 0.40 keeps a margin for the FSDP side, whose
      # observed peak was 42-45 GB reserved. vLLM sleeps during training so the
      # split is time-multiplexed, but the margin is what stops a step-0 OOM.
      gpu_memory_utilization: 0.40
    steps: 100
    eval_every: 10
    eval_n: 256
    save_every: 25
    # Keep optimizer semantics identical to the 2048 arm so only the token
    # cap differs in the paired experiment.
    warmup_steps: 10
    lr_schedule: cosine
    min_lr_ratio: 0.2

codecontests_rlvr_olmo31_32b_cap1024:
  _extends: codecontests_rlvr_olmo31_32b_cap2048
  max_completion_tokens: 1024
  dataset:
    soft_token_budget: 512
  training:
    verl:
      response_length: 1024
    eval_max_tokens: 1024

# Paid-compute boundary checks for the two long arms. They preserve the real
# 8-problem x 8-sample rollout shape and all cap/reward/optimizer settings;
# only run length and periodic eval/save cadence are disabled.
codecontests_rlvr_olmo31_32b_cap2048_smoke1:
  _extends: codecontests_rlvr_olmo31_32b_cap2048_long
  training:
    steps: 1
    eval_every: 0
    save_every: 0

codecontests_rlvr_olmo31_32b_cap1024_smoke1:
  _extends: codecontests_rlvr_olmo31_32b_cap1024_long
  training:
    steps: 1
    eval_every: 0
    save_every: 0

# Capacity fallback for the run-validated 2xB200 and 2xH200 TP2 profiles.
# These variants change hardware sharding only; science and cadence inherit
# from the corresponding long/smoke arm.
codecontests_rlvr_olmo31_32b_cap2048_b200x2_long:
  _extends: codecontests_rlvr_olmo31_32b_cap2048_long
  training:
    verl:
      n_gpus: 2
      rollout_tp: 2
      gpu_memory_utilization: 0.42

codecontests_rlvr_olmo31_32b_cap1024_b200x2_long:
  _extends: codecontests_rlvr_olmo31_32b_cap1024_long
  training:
    verl:
      n_gpus: 2
      rollout_tp: 2
      gpu_memory_utilization: 0.42

codecontests_rlvr_olmo31_32b_cap2048_b200x2_smoke1:
  _extends: codecontests_rlvr_olmo31_32b_cap2048_b200x2_long
  training:
    steps: 1
    eval_every: 0
    save_every: 0

codecontests_rlvr_olmo31_32b_cap1024_b200x2_smoke1:
  _extends: codecontests_rlvr_olmo31_32b_cap1024_b200x2_long
  training:
    steps: 1
    eval_every: 0
    save_every: 0

# Capacity fallback for a single 192 GB B200. This changes hardware sharding
# only and must pass the real-shape smoke before the long arm is launched.
codecontests_rlvr_olmo31_32b_cap1024_b200x1_long:
  _extends: codecontests_rlvr_olmo31_32b_cap1024_long
  training:
    verl:
      n_gpus: 1
      rollout_tp: 1
      gpu_memory_utilization: 0.42

codecontests_rlvr_olmo31_32b_cap1024_b200x1_smoke1:
  _extends: codecontests_rlvr_olmo31_32b_cap1024_b200x1_long
  training:
    steps: 1
    eval_every: 0
    save_every: 0

# 1-step plumbing check for the 32B path: proves the weights load, shard across
# 8 GPUs, and produce a graded rollout, without committing to a 100-step run.
codecontests_rlvr_olmo31_32b_smoke:
  _extends: codecontests_rlvr_olmo31_32b
  training:
    steps: 1
    batch_size: 2
    group_size: 4
    eval_every: 0
    save_every: 0
