# Hardware topology -> training.verl plumbing defaults (Ethan, 2026-08-10).
# Key = "<count>x<GPU>" as auto-detected from nvidia-smi at launch
# (infra/run_common.resolve_topology). Arm-level verl keys WIN over these;
# extra_overrides concatenates (topology first) instead — see apply_topology.
#
# Contract: every entry here has survived at least one smoke run on that
# silicon. A detected topology with no entry is a launch error on purpose —
# add the entry AND smoke it before the first paid run. Science knobs
# (prompt_length, response_length) stay in the arms.

# UNVALIDATED until the cs285_validate arms boot on it (their launch IS the
# smoke: a 1.5B full-FT single-GPU run, minutes to first signal).
1xB200:
  n_gpus: 1
  rollout_tp: 1
  gpu_memory_utilization: 0.42

# Validated: the whole AIME 32B campaign (2026-08-08..10).
2xB200:
  n_gpus: 2
  rollout_tp: 2
  gpu_memory_utilization: 0.42
  extra_overrides:
    - "++actor_rollout_ref.rollout.engine_kwargs.vllm.disable_custom_all_reduce=true"
    # Preload the immutable base checkpoint in vLLM, then sync only LoRA.
    # Dummy-load forces verl's first sync to gather and CPU-stage the full 32B
    # base; that hit the 351 GiB container RAM limit on a 2xB200 host.
    - "actor_rollout_ref.rollout.load_format=safetensors"
    - "actor_rollout_ref.rollout.layered_summon=true"

# UNVALIDATED — planned scale-up (2 TP2 engine replicas, FSDP over 4).
# Smoke before first paid use; per-GPU numbers copied from 2xB200.
4xB200:
  n_gpus: 4
  rollout_tp: 2
  gpu_memory_utilization: 0.42
  extra_overrides:
    - "++actor_rollout_ref.rollout.engine_kwargs.vllm.disable_custom_all_reduce=true"

# Validated for the 32B TP2 arms (util 0.42 sized against the 141GB card —
# the 0.50 attempt died at rollout boot, 2026-08-08).
2xH200:
  n_gpus: 2
  rollout_tp: 2
  gpu_memory_utilization: 0.42
  extra_overrides:
    - "++actor_rollout_ref.rollout.engine_kwargs.vllm.disable_custom_all_reduce=true"

# Validated by the CodeContests OLMo-3.1-32B TP4 phase/cap runs
# (2026-08-05..07). Those runs used 0.45 on 141GB cards; workload arms may
# deliberately override the fraction (the short-cap long arms use 0.40).
4xH200:
  n_gpus: 4
  rollout_tp: 4
  gpu_memory_utilization: 0.45

# UNVALIDATED — the first CodeContests short-cap smoke on 4xH100 is the gate.
# 0.40 x 80GB = 32GB for vLLM against ~16GB of TP4 bf16 weights; the measured
# FSDP-side peak on the same 32B workload was 42-45GB reserved. vLLM sleeps
# while FSDP trains, so these are time-multiplexed rather than additive.
4xH100:
  n_gpus: 4
  rollout_tp: 4
  gpu_memory_utilization: 0.40
  extra_overrides:
    - "++actor_rollout_ref.rollout.engine_kwargs.vllm.disable_custom_all_reduce=true"
