# CodeContests proposer-critic on OLMo-3-7B-Instruct-DPO, all-on-pod via verl.
# The code-domain sibling of math_pc_olmo.yaml: same machinery, same judge
# server, different task family and prompt pack.
#
# Flow: alice plans privately -> submits a program (public, graded) -> bob
# critiques -> alice defends -> bob rebuts (critic's last word) -> judge
# deliberates privately -> emits the verdict. Thinking is OFF (OLMo); the
# private scratchpad slots take its place.
#
# Judge = frozen Qwen3.5-4B on the pod's local vLLM (scripts/pod_run.sh derives
# model + port from THIS file). Standalone entries on purpose: protocol turn
# lists merge BY INDEX under _extends, which poisons mixed-shape protocols.
#
# Run (on the pod):
#   debate-rl --experiment-file configs/codecontests_pc_olmo.yaml \
#             --experiment codecontests_pc_olmo
#
# ---------------------------------------------------------------- sizing
# Unlike MATH, the problem statement dominates the context. Measured over the
# 365 usable train rows (approx tokens, chars/3.5):
#   description  median 474   p90 845   p99 1323   max 1850
# The judge's verdict context is the worst case in the protocol:
#   judge system card                     ~550
#   preamble: statement + program         ~2400   (p99 statement + a ~500tok program)
#   four public speeches at their caps     3072   (1536 + 512 + 512 + 512)
#   private deliberation                   1024
#   cues + verdict instructions            ~350
#                                        --------
#                                          ~7400  -> prompt_length 12288 with headroom
# response_length must cover the LARGEST slot cap (the proposal, 1536).
# max_token_len_per_gpu >= prompt_length + response_length.
#
# THESE ARE ESTIMATES, not measured on a live rollout. The smoke arm at the
# bottom exists to check them before a 100-step run: if verl reports truncation,
# raise prompt_length first, then max_token_len_per_gpu, then drop
# gpu_memory_utilization.
_includes: [_olmo_training.yaml]

codecontests_pc_olmo:
  _extends: _olmo_train_codecontests
  protocol:
    turns:
      - alice: [{name: scratchpad, visibility: private, max_total_tokens: 1024},
                {name: proposal, kind: solution, max_total_tokens: 1536}]
      - bob:   [{name: scratchpad, visibility: private, max_total_tokens: 512},
                {name: critique, max_total_tokens: 512}]
      - alice: [{name: scratchpad, visibility: private, max_total_tokens: 512},
                {name: defense, max_total_tokens: 512}]
      - bob:   [{name: scratchpad, visibility: private, max_total_tokens: 512},
                {name: rebuttal, max_total_tokens: 512}]
      - judge: [{name: deliberation, visibility: private, max_total_tokens: 1024},
                {name: verdict, kind: decision, max_total_tokens: 256}]
  prompt_config:
    file_path: infra/envs/debate/prompt_configs/codecontests.yaml
    entry: codecontests_proposer_critic
  fresh_positions: true
  agents:
    alice:
      trained: true
      model_settings:
        model_type: local            # served by the pod's verl rollout engine
        model_file_path: allenai/Olmo-3-7B-Instruct-DPO
        alias: proposer-alice
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    bob:
      trained: true
      model_settings:
        model_type: local
        model_file_path: allenai/Olmo-3-7B-Instruct-DPO
        alias: critic-bob
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
    judge:
      model_settings:
        model_type: local
        model_file_path: Qwen/Qwen3.5-4B
        alias: judge
        base_url: http://127.0.0.1:8788/v1
        # temp/top_p 1.0: the logit confidence channel is only honest here
        sampling: {train: {temperature: 1.0, top_p: 1.0}}
  judge_config: {schema_name: competitive, retries: 4}
  scoring:
    scoring: continuous
    confidence_source: json
    shaping:
      # +0.1 on the proposal DATUM when the family accepts its answer format;
      # for CodeContests that means the program came back in a properly CLOSED
      # fence.
      - {kind: format_reward, coeff: 0.1, slots: [proposal], flag: answer_format_valid}
  dataset:
    type: codecontests
    # Pinned JSONL, not a download: the loader drops rows the exact-output
    # verifier would mis-grade (multi-answer phrasing) — 458 -> 365 train,
    # 162 -> 116 test — so the training set is a function of these bytes.
    # Train/test share zero problem names.
    path: data/codecontests/train.jsonl
    test_path: data/codecontests/test.jsonl
    relaxed_extraction: true   # accept a fence truncated at the token cap
    seed: 0
    # Bounds a runaway program, not honest work. Measured over 632 real
    # completions: the slowest PASSING program takes 5.3s, and 30 -> 10 gave
    # 2.73x faster verification with 632/632 identical verdicts. 10s keeps ~2x
    # headroom over that 5.3s; 6s was 4.2x but only 1.4x headroom, which a
    # loaded or slower host could erase — a wrongly-timed-out correct program
    # is a silent label error, so the margin is worth more than the seconds. 30s x 116 eval rows / 8
    # concurrent verifiers ~ 7min worst case; the 90s default would be ~22min
    # per eval, 20 times over a 100-step run.
    timeout_seconds: 10
  training:
    verl:
      n_gpus: 1                      # H100/A100-80GB: 7B LoRA FSDP + colocated vllm + judge server
      gpu_memory_utilization: 0.33   # judge 0.18 + rollout 0.33 + 7B FSDP init peak must fit 80GB
      prompt_length: 12288           # see sizing note above (MATH needs 8192)
      response_length: 1536          # largest slot cap (the proposal)
      max_token_len_per_gpu: 16384   # >= prompt_length + response_length
    # RLVR eval: greedy CodeContestsEnv pass-rate on the held-out split, which
    # RUNS THE VERIFIER (unlike math's, which just parses a number).
    eval_max_tokens: 1536

# Tiny live-verification arm: proves the pod path end to end (judge server,
# verifier subprocesses, verl sizing) before committing to a real run.
codecontests_pc_olmo_smoke:
  _extends: codecontests_pc_olmo
  training:
    steps: 2
    batch_size: 2
    group_size: 4   # group 2 risks all-identical verdicts -> every datum dropped
    eval_every: 0
    save_every: 0
