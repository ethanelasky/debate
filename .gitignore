.env
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
wandb/
wheels/
checkpoints/

# Rescued run artifacts: LoRA checkpoints pulled off pods mid-run and the
# models merged from them. Multi-GB, and reproducible from the run that
# produced them plus scripts/merge_lora_ckpt.py.
artifacts/

# Built datasets: reproducible from scripts/build_codecontests_rlvr.py
# (the manifest pins revision, seed and sha256), too large to track.
data/

# Agent worktrees (created by isolation: worktree)
.claude/worktrees/

# MonitoringBench data + results: local for now by user decision (2026-08-01), not yet committed
data/monitoringbench/
outputs/
