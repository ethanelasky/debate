.env
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
wandb/
wheels/
checkpoints/

# Built datasets: reproducible from scripts/build_codecontests_rlvr.py
# (the manifest pins revision, seed and sha256), too large to track.
data/

# Agent worktrees (created by isolation: worktree)
.claude/worktrees/

# MonitoringBench data + results: local only. The attack trajectories are
# redistributed from the MonitoringBench HF release and this repo does not
# establish a right to republish them.
data/monitoringbench/
outputs/

# Verifier benchmark corpus: 5.2 MB of CodeContests problems and solution
# programs derived from deepmind/code_contests. Rebuildable; not ours to ship.
bench/verifier_cases.jsonl
