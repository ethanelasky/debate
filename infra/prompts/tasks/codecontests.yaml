# CodeContests answer-generation prompts, reproducing the OLD repo's
# codecontests.yaml `answer_generation_user` composition: the "Context:" frame,
# the <question> block, the Notes (from `judge_answer_generation_user`,
# verbatim), and the "Provide your answer:" cue. The old
# <background>/<grading_criteria> sections are dropped: this repo binds the
# whole problem as the question and never shows debaters the test cases. The
# two sentences closing answer_gen_system are verbatim from the old repo's
# shared.yaml "Direct Code Generation" prompt.
#
# THIS FILE IS THE SINGLE SOURCE for the codecontests answer prompt, for BOTH
# arms. The RLVR user message and the debate proposal slot are the SAME
# composition rendered byte-for-byte identically: DebateEnv splices
# answer_gen_user into the proposal slot wherever
# infra/prompts/debate/codecontests.yaml writes <ANSWER_GEN_USER>,
# binding the problem text to that pack's <TOPIC>. Editing the wording here
# changes both arms together, which is the point — do not restate it there.
# math.yaml works the same way; the two families are not treated differently.
#
# <PROBLEM> is substituted with the row's problem description.
#
# DELIBERATE deviation from old-repo verbatim (Ethan, 2026-08-03): the
# "competitive" qualifier is dropped ("competitive programming problem" ->
# "programming problem") — CodeContests problems ARE named Codeforces
# problems, and contest framing coaches the model toward recalling memorized
# solutions instead of reasoning in-context. It is the only
# placeholder the RLVR arm binds, so nothing else here may be uppercase-tagged.
format_notes: |-
  - Write a standalone Python script that reads from stdin and prints to stdout.
  - Write your answer in ``` blocks. Example:
  ```python
  # your code here
  ```
  - Read input from stdin using input() or sys.stdin.
  - Print output to stdout using print().
  - Do NOT wrap your solution in a function or class unless the problem requires it.
  - Import any libraries you use.
answer_gen_system: |-
  Solve the programming problem.
  Your goal is to write code that is consistent with the specification in order to pass the hidden test cases. Do not include functionality or error handling beyond what is described.
answer_gen_user: |-
  Context:

  <question><PROBLEM></question>

  Notes:
  <FORMAT_NOTES>

  Provide your answer:
