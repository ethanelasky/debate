"""Config viewer: side-by-side comparison + editing of prompt-library YAML
(infra/prompts/debate/*.yaml) and experiment configs
(configs/*.yaml). See TOOLS_VIEWER_SPEC.md.

Run: python -m tools.viewer [--port 8080] [--no-browser]
"""
